{
  "name": "physics-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": { "astro": "^5.13.0", "@astrojs/sitemap": "^3.5.1" }
}
